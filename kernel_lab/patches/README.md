# Patch Plans

This folder stores human-readable patch plans for future kernel candidates.

These files are not automatic edits to upstream code. They are small specs that
explain:

- what kernel boundary should change,
- what proof-validity gate must pass,
- what benchmark window is required,
- why the candidate is different from prior attempts.

Keep patch plans free of wallet addresses, worker names, local paths, and raw
logs.
